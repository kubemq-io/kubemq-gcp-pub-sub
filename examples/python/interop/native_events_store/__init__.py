"""interop/native_events_store — Pub/Sub publish -> native Events Store consume.

Run with either form (spec S5.6):

    uv run python -m interop.native_events_store
    uv run python interop/native_events_store/main.py
"""
