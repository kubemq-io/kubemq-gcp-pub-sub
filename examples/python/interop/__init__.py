"""Interop examples for the KubeMQ Google Pub/Sub connector (Python).

The single ``native_events_store`` variant — the only example in the repo that
imports a KubeMQ SDK — demonstrates a Pub/Sub publish consumed natively from the
topic's Events Store log ``pubsub.{topic}``. See ``native_events_store/README.md``.
"""
